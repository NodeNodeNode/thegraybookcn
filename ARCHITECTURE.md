# 设计与踩坑

这份文档记录**为什么这么做**，以及**哪些坑已经踩过**。

文风与排版规则在 [`TRANSLATION-STYLE.md`](TRANSLATION-STYLE.md)，术语在 [`translation/terms.yml`](translation/terms.yml)。这里不重复那两份，只写它们背后的设计，以及那些「不看这份就会重新踩一遍」的东西。

---

## 一、这个项目要解决的真问题

翻译一本还在持续更新的书，难点从来不是翻译本身，而是**三种漂移**：

| 漂移 | 症状 | 对策 |
|---|---|---|
| 上游漂移 | 原文改了，译文还停在旧版，读者看到的是过时信息 | blob 账本（§2） |
| 术语漂移 | 同一个词在第 3 章叫「草图」、第 9 章叫「图档」 | 术语表三层机制（§4） |
| 结构漂移 | 上游插了一页，我们的目录还是老的；锚点深链悄悄断掉 | 从 toc 生成侧边栏（§3）、锚点门槛（§5） |

三者的共同点是**失败无声**：不报错、不断链、构建照过，只是内容悄悄烂掉。所以本项目的每一条机制，目标都是**把无声的失败变成吵闹的失败**。

这条原则值得单独强调，它解释了后面很多看起来「过度」的设计：

> 宁可多报一次（吵），也不能悄悄漏一次（哑）。

---

## 二、上游同步：blob 账本

每一页译文的 frontmatter 里带四个账本字段：

```yaml
source_path: reference/language/patches.md   # 与上游 1:1 的路径，同步主键
source_blob: fe98615c6f5ac2d95587eab574abd6f3c7427e9e   # 翻译时上游那个文件的 git blob SHA
status: translated                            # translated | partial | stub
last_synced: '2026-08-12'
```

### 为什么用 blob SHA 而不是仓库 HEAD

上游 HEAD 每周都在变，但那和「我翻的这一页变没变」毫无关系。**blob SHA 是文件内容的指纹**，只在这个文件真被改动时才变。用 HEAD 会让 135 页天天全部「过期」，那等于没有信号。

### 为什么 `docs/` 的路径必须与上游 1:1

路径就是同步主键。URL 由每页 frontmatter 的 `slug` 单独决定，与路径解耦 —— 所以路径可以专心服务于同步，URL 可以专心服务于读者。这两件事一旦混在一起，改 URL 就会打断同步。

### 上游副本不进仓库

`.upstream/` 是 gitignored 的部分克隆（`--filter=blob:none`：拿完整提交历史以支持重命名检测，blob 按需拉取）。上游正文一个字都不会进我们的仓库。

### 日常同步流程

```bash
npm run gb:upstream   # 更新 .upstream/
npm run gb:status     # 谁过期了、谁还没翻
```

`gb:status` 对每个过期页给出它在上游的改动历史链接。想看**具体改了什么**用 `npm run gb:diff` ——
它会沿提交历史回溯到我们那一版对应的 commit，给出可用的 compare 与本地 diff 命令。

⚠️ 这里有个坑：账本里记的是 **blob**，而 GitHub 的 `/compare/` 只接受 commit。
曾经的实现直接把 blob 拼进 compare URL，等于把维护者第一个会点的链接做成了 404。

**实测漂移率约 6 页 / 3 个月**（拿 2026-04-22 的上游提交做过对照实验）。

上游更新之后的完整跟进流程（含自动巡检、判断表、回填账本）见 [`MAINTENANCE.md`](MAINTENANCE.md)。

---

## 三、侧边栏与四板块导航：从上游 toc 生成

`sidebars.js` 由 `npm run gb:gen-sidebar` 从上游 `toc.md` 生成，**不要手改**。

### 为什么不用 Docusaurus 的 autogenerated

- `autogenerated` + `sidebar_position` 要求人工维护 135 个序号，上游每插一页就得重排
- 更要命的是：`introduction/` 那一章的**人类可读标题只存在于 `toc.md` 里**。文件名是 `lo_9_2_Spreads.md` 这种，除了解析 toc 别无来源

中文标签维护在 `translation/sidebar-labels.json`，重新生成不会覆盖。

### 四个板块自己会亮

头部四个板块（参考手册／概念讲解／版本变更／路线图）的状态由 `translation/navbar-sections.json` 决定，而那份文件也是生成的：

- 某个板块**一页译文都没有** → 头部那一项显示为「（英文）↗」，直接链上游官网，读者不会撞空
- 翻了之后重新生成 → **自动**转为站内链接

整个过程从头到尾没有手改过一行配置。第一次看到它工作是在概念讲解章翻完时。

---

## 四、术语防漂移：三层机制

唯一真源是 `translation/terms.yml`。`TERMINOLOGY.md` 是它的产物（`npm run gb:terms-doc` 生成），**不要手写那一份**。

三层机制缺一不可：

| 层 | 时机 | 工具 | 作用 |
|---|---|---|---|
| 1. 事前注入 | 翻某页之前 | `gb:termcard <上游路径>` | 生成**这一页用得上**的小术语表 |
| 2. 事后校验 | 翻完之后 | `gb:check-terms` | T1–T4 + E1 规则 |
| 3. 棘轮规则 | 提交时 | 人（硬约束） | 新决定的术语必须在**同一个 commit** 里进表 |

### 为什么术语卡不注入全表

全量术语表随翻译推进只会越来越长，翻到后面必然读不完也记不住。按当前页原文求交集得到的小表，**成本恒定**，而且永远只包含用得上的词。

### 棘轮规则是最重要的一条

「翻某页时新决定的术语，必须在同一个 commit 里写进 `terms.yml`。没进表，这页就不算翻完。」

术语表只增不减，`gb:check-terms` 是逼它生长的那个力。没有这条，前两层都会慢慢失效 —— 因为最难的不是检查已知术语，而是**让新术语被记下来**。

### `forbid` 字段的意义

术语漂移只有在你枚举了「错的长什么样」之后才是可检测的。每发现一处不一致，就把错误译法补进对应条目的 `forbid`，从此它再也不会重新出现。

### 拍板归属

**「某个词保留英文还是译成中文」由 RED 定**，具体译法和检查规则不用问。原因：这些词是 vvvv 中文社区的口语习惯，社区共识只有他知道。

但**仓库里已有明显多数派用法的词，按事实定即可，不必再问** —— 那不是新决定，是把已经发生的事记进表。例如 `shader` 定为「着色器」，依据是现有译文 42 处「着色器」对 17 处裸用；`slice` 保留英文，依据是 16 处全裸用、0 处「切片」。

---

## 五、锚点：钉死上游值

我们的标题是中文，上游是英文，两套 slug 算法（Markdig vs github-slugger）必然分叉。做法是**给每个标题钉死显式 id**：

```markdown
## 数据与数据枢纽 {#data-and-data-hubs}
```

于是外部指向上游锚点的深链在中文站上照样能落地。

`npm run gb:check-anchors` 验证的是：**上游每一个带 `#anchor` 的内部链接，是否都能解析到目标文件里的某个真实标题**。对得上就钉死，两套算法的差异从此不重要；对不上则要么是上游的死锚点、要么是算法真的分叉，两种都必须人工看一眼。

⚠️ **注意分工**：锚点门槛查的是**上游锚点**，不查我们**站内的 slug**。站内 slug 猜错要靠构建器的 `onBrokenLinks` 抓（见 §7）。

---

## 六、已经踩过的坑

按「会不会再踩」排序，最上面的最危险。

### 6.1 强调标记紧贴汉字会静默失效 ⚠️ 最常踩

CommonMark 的 flanking 规则：收尾 `*` 若前面是标点，后面必须是空白或标点才能闭合。

```
✗ *VL Document（VL文档）*中     收尾星号前是全角）、后接汉字 → 不闭合
✗ **VL Document（VL文档）**中   加倍星号受同一规则约束，绕不开
✗ 请**[联系我们](url)**，       开标记前是汉字、后是 [（标点）→ 根本不会开启
✗ *「直接使用 .NET 库」*这个    直角引号同理
✓ *VL Document*（VL文档）中     收尾星号跟在拉丁字母后，结构上不可能触发
```

**后果**：不报错、不断链、构建照过，只是安静地把裸星号漏给读者。曾经全站 18 处这样漏着。

**CSS 救不了** —— 那些标记从未被解析成标签，样式无从附着。

**约定形式**：`*English*（中文）`，中文一律移到标记外。已踩过的变体：全角右括号、方括号、直角引号。

**渲染**：`em` 在 `src/css/custom.css` 里定义为 `font-style: normal; font-weight: 600`。中文字体（PingFang SC / 思源黑体 / 微软雅黑）**没有一款有斜体字形**，浏览器只能做合成倾斜，把方块字剪切歪斜；中文排版传统里也没有斜体这个强调层级。

**检查规则 E1** 拦这个，级别 error。

### 6.2 E1 的实现：不要手写 flanking 判定

E1 最初是手写的 flanking 配对，把 `**必须写回 *Pad*（数据板）**` 这种**正确的嵌套**报成了 error（原文后来因 Pad 改为保留英文而调整，这里保留当时的形态） —— CommonMark 的定界符配对**带栈**，手写正则追不上。

现在改为**直接问 micromark**（Docusaurus 自己用的解析器）：渲染完还剩星号，才算真漏。判定标准从「我认为的规则」变成「渲染器的实际行为」，没有分歧空间。

配套的两条修正，都是 E1 自伤：

- **转义星号 `\*`** 渲染出来就是字面星号，不参与配对 —— 不排除会被当成「漏出来的」
- **剥列表符号时不能保留缩进**：`   * 文字` 若剥成 4 个空格开头，那在 CommonMark 里是**缩进代码块**，整行按字面输出，星号全被误判

### 6.3 `maskCode` 只认单反引号（影响 T1–T4，不止 E1）

```
``ShadowCaster*``    ← 双反引号
```

单反引号的正则会把开头那对 `` ` `` 当成一个空代码段吃掉，**中间内容整个漏进正文**当散文检查。这是 T1–T4 共用的遮罩，影响面比看上去大。现在的正则认多重反引号。

### 6.4 批量改术语大小写会顺手改掉显式锚点

用正则批量把 `delegate` 改成 `Delegate` 时，`{#delegate}` 这个**显式锚点也会被改**，静默断掉全站深链。

**信号**：检查器报 12 处，脚本却替换了 13 处 —— **对不上数就是信号**。

现在的做法是替换前后比对锚点列表，不一致就断言失败。

### 6.5 Docusaurus 的 `numberPrefixParser`（构建直接失败）

Docusaurus 默认会把文件名开头的「数字＋点」当作**排序前缀剥掉**。于是：

```
5.x.md  6.x.md  7.x.md   →  doc id 全变成 `x`  →  构建报 "docs sharing the same id"
2020.1.md → id `1`，2021.4.md → id `4`   ← 一直在悄悄糟蹋，只是恰好没撞车
```

我们的文件名与上游 1:1、顺序完全由 toc 决定，这个机制有害无益。已在 `docusaurus.config.js` 里 `numberPrefixParser: false`。

### 6.6 一文多挂时，降级 link 的 href 不能用文件路径

上游 toc 里 `text-rendering.md` 被挂了两处（3D 图形下、最佳实践下）。生成器对第二次出现降级成 `link`，但那条 link 的 href 原先用的是 **docId（文件路径）**。本项目每页都有显式 frontmatter `slug`，**文件路径从来就不是有效地址**。

**症状**：死链出现在那一章**每一页的侧边栏**上。构建器提示「Frequent broken links … appear on all pages through your site layout」—— 那句话是关键线索，正文里逐页找是找不到的。

现在改为从译文 frontmatter 读真实 slug。

### 6.7 站内 slug 靠构建器抓，不靠锚点门槛

我曾把 `/hde/commandline-arguments` 猜错（实为 `/develop-environment/commandline-arguments`）。锚点门槛查的是上游锚点，不管站内 slug；是构建器的 `onBrokenLinks: throw` 逮到的。

**做法**：翻完一章后，用脚本把该章所有 `](/...)` 形式的站内链接与全站实际 slug 比对一遍，别等构建器一个个撞。

### 6.8 `.mdx` 页会被生成器当成「未翻译」

上游路径一律是 `.md`，但需要 Tabs 之类组件的译文只能是 `.mdx`。生成器原先只按 `.md` 去 `docs/` 找文件，找不到就把那页降级成英文外链。现在两种扩展名依次找。

### 6.9 老 `partial` 页往往不是「没译完」，而是译错了

标着 `partial` 的四年前老译文，翻到时**先验 blob**：

```bash
git -C .upstream rev-parse HEAD:<source_path>
```

若与 frontmatter 里记的一致，说明**原文没变、是译文的问题**，应整页重译而非补完。实际发现的：

- `3d/shaders` 把「可以照常用 `#include`」译成了「并没有 `#include`」——**意思正好相反**
- `reactive` 把图注和清单合并成一行，**图注整个丢了**；标题层级也压平了
- `writing-nodes` 中文 272 字 / 上游 2570 词，内容停在 Visual Studio 2019

### 6.10 只看输出末尾会漏掉错误计数

我曾用 `gb:check-terms | tail -2` 看结果，只看到「待拍板术语 N 个」，**漏掉了 11 个既存 error**。

**做法**：每批翻译后必须看**计数行**（`检查 N 个文件：X error，Y warn`）。

### 6.11 YAML 会把纯数字的 blob 解析成 number

frontmatter 里的 SHA 没加引号，YAML 会把纯数字的值解析成 number。40 位十六进制恰好全是数字的概率约 8e-9，实践中几乎不会遇到 —— 可一旦遇到，`entry.blob &&` 会在值为 0 时短路，**漂移被静默漏掉**。

已在 `gb-status.mjs` 里强制 `String()` 后再比。最坏情况是多报一次，而不是漏报一次。

### 6.12 大小写要按语境分，不能统一替换

同一个词在同一章里可能有两种含义：

| 写法 | 何时 | 例 |
|---|---|---|
| `Record` / `Class` | VL 的草图类型 | 「不再为 Record 生成代理类」 |
| `` `record` `` / `` `class` `` | C# 关键字 | 「更好的 C# `record` 支持」 |

`keywords.md` 里的 `### class`、`### delegate` 是 C# 关键字本身，**小写才对** —— 那里用 `<!-- gb-ignore T1 ... -->` 抑制，而不是改写。

### 6.13 通配节点名里的星号

`VideoSourceTo*`、`File*` 里的星号是字面意思，但裸写会被 CommonMark 漏成裸星号。按 §6 当作代码名加反引号。

---

## 七、上游本身的错漏

上游是 DocFX + Markdig，有一批我们必须处理的问题。**遇到断链不要默默改指**，加 `:::note[译者注]` 说明原委。

### 上游根本不存在的文件（断链）

```
introduction/data.md              ← lo_0_dataHubs 指向它
libraries/graphics-2d.md          ← text-rendering 指向它
libraries/io.md                   ← raspberry-pi 指向它
libraries/projectionmapping.md    ← 在 toc 里但文件不存在
```

### 只有标题、没有正文的空壳（**不造空壳**）

```
introduction/more-on-data.md   introduction/generics.md
introduction/nodes.md          reference/language/generics.md
```

### 不在任何 toc 里的页面（官网点不到，**不翻**）

`reference/best-practice/ptp.md`、`reference/hde/inspector.md` 等 17 个。`gb:status` 会单独列出来，不计入分母。

### 上游渲染出来就是错的

- `custom-regions` 用 `** \`None\` - ...` 想写嵌套列表，但 `**` 后无空格，CommonMark 不认
- `contributing` 的 `--editable-packages VL.Audio;VL.Devices*"` 尾部多一个引号
- `7.x` 的 `[Public Channels]` `[Presets & Transitions]` `[Bindings]` 写成方括号却没给目标
- `texturefx` 的 `[Includes](shaders.md#includes)` 是过期锚点（那节现已改名）

### 标题层级不规范

- `custom-regions` 有**两个 h1**
- `gpu-debugging` 以 **h2 开篇**，没有 h1
- `introduction/` 那一章的小节**全是 `####`**，直接挂在 h1 下

**处理原则：保持相对层级，而不是绝对层级。** h1 交给 frontmatter 之后，整体提一级，让正文从 `##` 起。锚点仍钉上游原值 —— 锚点与标题级别无关。

---

## 八、翻一章的标准流程

```bash
# 0. 摸清这一章：页数、词数、哪些是老 partial
npm run gb:status

# 1. 老 partial 先验 blob，判断是补完还是重译
git -C .upstream rev-parse HEAD:<source_path>

# 2. 翻某页之前，生成这一页的术语卡
npm run gb:termcard <上游路径>

# 3. 翻。新术语同 commit 进 translation/terms.yml（棘轮规则）
npm run gb:terms-doc          # 重新生成 TERMINOLOGY.md

# 4. 补 translation/sidebar-labels.json，重新生成侧边栏
npm run gb:gen-sidebar

# 5. 四道门槛，一道都不能跳
npm run gb:check-terms        # ← 看计数行，不要只看 tail
npm run gb:check-anchors
npm run build                 # onBrokenLinks + onBrokenAnchors 双 throw
#   外加：站内 slug 比对、图片可达性、条目计数比对（见下）
```

### 值得额外做的三项核对

| 核对 | 怎么做 | 抓到过什么 |
|---|---|---|
| 站内 slug | 把该章所有 `](/...)` 与全站实际 slug 比对 | `/hde/commandline-arguments` 猜错 |
| 图片可达性 | 对新引用的图片 URL 逐个 `curl -I` | 上游缺图的页面 |
| **条目计数** | 比对上游与译文的**列表条目数** | changelog 章漏译 1 条 |

最后一项在条目密集的章节（changelog 单页 373 条）几乎是唯一可靠的完整性检查 —— 靠眼睛读一遍是发现不了漏掉一行的。

---

## 九、几条不要重新讨论的决定

- **不造空壳。** 上游只有标题没有正文的页面，不创建对应译文
- **不翻导航外页面。** 官网点不到的，`gb:status` 单独列，不计入分母
- **`docs/` 路径与上游 1:1。** URL 由 frontmatter `slug` 决定，两件事解耦
- **`TERMINOLOGY.md` 和 `sidebars.js` 是生成物。** 改真源再生成
- **`em` 是「术语首现」的专用信号。** 上游用斜体做一般强调的地方（比如给同一个词连加 8 次），不保留 —— 中文里那会变成噪音
- **原创内容标 `origin: cn`**，不计入上游覆盖率分母
