# 灰皮书中文版翻译风格指南

这份指南不是从零发明的规范，是把 RED 已有译文里的既定习惯提取出来写死。
后续每一页都要和这些保持一致，否则整本书读起来会分裂。

术语查 [`translation/terms.yml`](translation/terms.yml)，那是唯一真源。本文只管文风与排版。

这些规则**为什么长这样**、以及哪些坑已经踩过（比如 §5 那条约定背后的 CommonMark flanking 陷阱），见 [`ARCHITECTURE.md`](ARCHITECTURE.md)。

---

## 1. 语气与人称

- **第二人称一律「你」，禁用「您」。** 现有译文 0 处「您」，这是硬约定。
- 叙述时可以用「我们」拉近距离：「草图是我们放置节点的地方」。
- **意译为主，不要贴着英文语序走。** 可以重组句式、拆分长句、加语气词。这本书的既有语感是口语、亲切、非正式的。

正例（摘自现有译文）：

> 与其单打独斗成为我变秃了也变强了的超人，不如借助社区的力量……岂不爽哉。

**翻译腔黑名单** —— 出现这些基本说明在直译：

- 「进行……」「使得……」「通过……的方式」「对……进行……」
- 「一个……的……的」多层定语堆叠
- 无必要的被动句（英文习惯被动，中文习惯主动）
- 「其」「该」「此」滥用（除非是正式的参考型内容）

## 2. 段落与句长

- 段落 1–3 句，写完就换。现有译文几乎没有长段。
- 英文长句强制拆分成中文短句。
- 能用无序列表就用列表，这本书的既有形态就是列表密集。

## 3. 标题结构

- **h1 交给 frontmatter 的 `title`，正文从 `##` 起。** 全库 0 处正文一级标题，这是硬约定。
- 只用 `##` 和 `###`，不跳级。
- **上游的 `####` 一律并入 `###`。** 上游自己就跳级（`reference/language/nodes.md` 有 `####`，`introduction/lo_2_dataflow.md` 从 h1 直接跳 `####`），这条规则必须预先定死，否则结构校验天天误报。
- **全书双语标题**：`English / 中文`，如 `## Datatype Patch / 类型草图`。专有名词单独成标题时保持原样（如 `title: VL`）。
- **每个标题都要钉显式 id**，值用上游的英文 slug：

  ```markdown
  ## 节点浏览器 {#the-node-browser}
  ```

  理由：上游带锚点的深链、论坛引用、搜索引擎收录都指着这些英文锚点。钉死之后，
  以后润色中文标题也不会断链。`onBrokenAnchors: 'throw'` 会在构建期强制这件事。

## 4. frontmatter 白名单

只允许这些字段，多一个都不要：

```yaml
---
title: The NodeBrowser / 节点浏览器
slug: /develop-environment/the-node-browser
source_path: reference/hde/the_nodebrowser.md
source_blob: 3f9a1c…
status: translated
last_synced: 2026-08-12
---
```

审校状态记在 `translation/mapping.yml`，**不进 frontmatter** —— 那些是过程信息，不该发布到站点。

中文站原创页（不对应任何上游文件）标 `origin: cn`，同步脚本据此跳过。

## 5. 术语首次出现

标了 `first_use: true` 的术语，在**每页**首次出现时标记英文、中文放在标记外的括号里，之后全用中文：

```markdown
草图同样也可以包含 *Regions*（区块）。
```

**中文必须在标记外面，这不是审美问题，是解析问题。** CommonMark 要求收尾的 `*` 满足
right-flanking：它前面若是标点，后面就必须是空白或标点。写成 `*Regions（区块）*的` 时，
收尾星号前面是全角 `）`、后面紧跟汉字 —— 无法闭合，页面上会原样显示两个裸星号。
而中文排版「全角括号内外不加空格」的习惯，让这个陷阱几乎必然踩中。

`**加粗**` 受同一条规则约束，把星号加倍并不能绕开。

写成 `*English*（中文）` 则永远安全：收尾星号总是跟在拉丁字母后面。

**渲染效果是粗体，不是斜体。** 中文字体没有斜体字形，浏览器只能做合成倾斜，把方块字
剪切歪斜，既难看强度又不够。`src/css/custom.css` 里已经把 `em` 定义为
`font-style: normal; font-weight: 600`。

检查器的 **E1** 规则会拦截所有闭合失败的写法，级别是 error —— 它抓的是渲染 bug，
不是风格偏好：这类错误不报错、不断链、构建照过，只是安静地把标记漏给读者看。

> 这条约定在 2026-08-12 重定过。此前写的是 `*English（中文）*`，归纳自 2022 年
> 单个文件里的一段用法，样本不足且当时就有一半是坏的。按实测重定后，全库 29 处
> 标记一并改齐。

## 6. 代码与界面元素

- 快捷键、菜单路径、文件名、后缀、针脚名：**单反引号**。
  `` `F1` ``、`` `Quad > Extensions` ``、`` `.HDE.vl` ``
- **组合键统一写成** `` `Ctrl` `+` `P` ``。现有译文有 `` `Alt``P` ``、`` `CTRL` `+` `` 等多种写法，以后一律按前者。
- 上游的 `<kbd>` 和 `<span class="keyseq">` 原样保留 —— 样式已经在 `src/css/custom.css` 里实现了。
- 粗体只用于强调，**不要**用来标记界面元素。

## 7. 上游 DocFX 标记的转换

`gb:import` 会自动做掉大部分，但你要知道对应关系：

| 上游（DocFX / Markdig） | 本站（Docusaurus） |
|---|---|
| 正文 `# 标题` | frontmatter `title` |
| `> [!NOTE]` | `:::note` |
| `> [!TIP]` | `:::tip` |
| `<center>图注</center>` | 图片下一行的中文图注 |
| `<center>` 布局用途 | `<p align="center">` |
| 相对图片 `../../images/…` | `https://thegraybook.vvvv.org/images/…` |
| 相对链接 `x.md#anchor` | 本地已翻则用本地 slug，未翻则链上游 `.html` |
| `\(` `\)` 转义括号 | 普通括号 |

## 8. 译者补充

这本书的一个特色体例，要保留并制度化：

- 短补充用 `:::note`。
- 成段的个人见解用文末 `---` 分隔 + `## 个人笔记`。`docs/language/nodes.md` 末尾用黑胶唱片和磁带比喻 Record 与 Class 就是范例。

**硬规则：任何译者原创内容必须在视觉上可与原文区分，不得混进正文冒充原文。**

## 9. 标点

- 中文正文用全角 `，。：；（）`。
- 文件后缀、路径、版本号周围保持半角：`.vl`、`.NET`、`2021.4`。
- 引号沿用现有的 `“ ”`。
- 不用书名号《》。术语首现用 `*...*` 标记（渲染为粗体，见 §5），不用书名号或引号代替。

## 10. 中英文之间加空格

**全书统一加空格。**

```
在 patch 中，节点就是主要的创建模块     ← 对
在patch中，节点就是主要的创建模块       ← 错
```

例外：

- 中文与半角标点之间不加。
- 全角括号 `（）` 内外不加。
- 数字与中文之间**加**。

这条**交给 `gb:fmt` 自动处理，不要靠人工自查** —— 任何需要每页自查的排版规则最终都会失守。

## 11. 不译清单

产品名、节点名、菜单项、快捷键、类型名一律保留英文：
`vvvv`、`VL`、`Stride`、`Skia`、`NuGet`、`Quad`、`IOBox`、`Record`、`Class`。

具体每个词查 `terms.yml` 的 `status: keep-en`。

## 11.5 标题采用「English / 中文」

**英文是正名，中文是释义。** 上游的标题文字原样放在前面，中文译名跟在 ` / ` 后面，锚点保持英文不变：

```markdown
## Data Hubs / 数据枢纽 {#data-hubs}
## Operation Applications / Operation 应用 {#operation-applications}
```

理由与 `terms.yml` 里 `Pad`、`Operation` 保留英文是同一条：读者在 vvvv 界面、论坛、上游文档里遇到的是英文，中文只是帮助理解。只给中文，读者记不住那个词，也没法拿它去搜索。RED 2026-08-14 拍板。

**四类例外，保持现状不加英文：**

1. **中文半边就等于英文** —— `## ForEach {#foreach}`、`## Spread {#spread}`。加了会变成 `ForEach / ForEach`。
2. **通用词** —— `## 用法 {#usage}`、`## 排查问题 {#troubleshooting}`。这类词没有记忆价值，加英文只是噪声。
3. **中英两边会带同一个代码片段** —— `` `csproj` file / `csproj` 文件 `` 里那个 token 出现两次。中文半边已经把英文露出来了，保持纯中文。
4. **非译自上游的页面**（README、翻译说明、术语表等）—— 没有英文原文可依。

**英文半边从上游取，不要自己写。** 靠锚点去 `.upstream/` 里匹配对应标题，取它的原文。上游有拼写错误（`Limitiations`、`Github`）时改正 —— 锚点仍保留上游的错拼，追溯不受影响。

这条不进检查器：例外靠判断，机器分不清「通用词」和「实义术语」。

## 12. 链接规范

- 站内链接用相对 `.md` 路径，Docusaurus 会解析成最终 URL（我们的目录与上游 1:1，所以上游的相对链接基本可以直接沿用）。
- **目标页尚未翻译时**：链到上游英文原页，并在链接文字后加「（英文）」。

  ```markdown
  详见[导出应用（英文）](https://thegraybook.vvvv.org/reference/hde/exporting.html)。
  ```

  不留死链、不退化成纯文本。这些链接可以被脚本枚举，目标页译完后批量回填。
- 每页顶部统一给出原文链接（`gb:import` 自动生成）。

## 13. 检查器抑制

误报时用抑制注释，**理由必填**：

```markdown
<!-- gb-ignore T1 这里的 class 指其他编程语言里的通用概念，不是 VL 的 Class 类型 -->
它基本上可以理解为在其他编程语言中的“class”。
```

注释独占一行则作用于下一行，跟在正文后面则作用于本行。省略检查码则抑制该行全部。

**别为了让检查器变绿而改一个本来正确的句子** —— 那是本末倒置。

---

## 14. 去翻译腔

下面六条是全站实测出来的高频症状，例子**全部取自本仓库自己的译文**，不是编的。

`npm run gb:prose-smell` 能按「它」密度给文件排序，帮你先看最重的那些。但它只定位，不判决 —— 每一条都要人读过才算数。

### ① 「它/它们」照搬英语代词

英语靠代词维持指代，中文靠**话题链**：话题一旦立住，后面的主语能省就省。

```
✗ 区块……：它们做某件具体的事 —— 这一点上它们像节点；但它们对细节「心里没底」，
  于是把最终用户请进来 —— 这一点让它们成为区块。
✓ 区块……：做某件具体的事 —— 这点像节点；可对细节心里没底，得把用户请进来补上
  —— 这点让它成为区块。
```

4 个「它们」变 1 个，意思一点没少。

### ② 「让…可以 / 使得 / 允许你」

英语 allow / enable / let 的直译。中文直接说谁做什么。

```
✗ 基于 Delegate 的区块，让区块的设计者可以把任意数据送进区块内部
✓ 基于 Delegate 的区块，设计者可以把任意数据送进内部
```

### ③ 「……的方式，是……」

What X is to Y 的直译。

```
✗ 我们想在代码里使用这样一个枚举的方式，是把它作为某个 Operation 的输入参数的类型
✓ 这种枚举的用法是：把它当作某个 Operation 输入参数的类型
```

### ④ 被动式

中文的「被」常带不如意色彩，技术文里能主动就主动，或者用「会」「要」带过。

```
✗ 只有条件成立时，这个区块的主体才会被执行。
✓ 只有条件成立，区块主体才执行。
```

被动确有必要时（强调受事、施事不明）当然可以用 —— 这条不是禁令。

### ⑤ 长定语前置

英语关系从句照搬成「……的 + 名词」，中文读起来喘不过气。拆成短句，或者用破折号后置。

```
✗ 一种回头去调用区块内部那张小草图的办法，而那张小草图是由区块的最终用户打的
✓ 一种回头去调用区块内部那张小草图的办法 —— 那张草图由用户自己打
```

### ⑥ 「一个 X 的 Y」（a Y of X）

```
✗ 每一个实体都带有一个组件的序列      ✓ 每个实体都带着一串组件
```

### 底线：不要过度归化

技术文档的**首要义务是准确**，不是好听。拿不准的时候保留直白，不要为了「像中文」而改变意思、或者自作主张补充原文没有的内容。

润色时用 `npm run gb:check-rewrite` 断言只改了散文 —— 锚点、链接、代码块、列表条目数、标题层级一个都不许动。

---

## 翻一页的流程

1. **扫描**上游原文，先产出纲要卡：标题树、图片清单、代码块、链接、贯穿全文的示例名、新术语候选。不翻译。
2. **术语裁决前置**：新候选逐个定「译 / 保留英文 / 待问」。不确定的标 `<!-- Q: … -->`。
   顺序不能反 —— 译到一半才改词，前半页要返工。
3. **分段翻译**：按 `##` 切，每批 800–1200 英文词。
4. **图片与图注**。
5. **链接改写**。
6. **通读收尾**：指代、口吻、重复用词、段落长度、接缝。分批翻译在段落交界处一定有接缝，只有通读能抹平，这步不能省。
7. **自检**：跑 `npm run gb:check-terms -- <文件>`，再人肉三问 ——
   有没有一句我自己读不通？有没有一处是照着英文语序走的？标题数和图片数与上游对得上吗？

### 三条禁令

1. **不发明原文没有的技术断言。** 不确定就标 `<!-- Q: … -->`，不猜。
2. **个人补充不得混入正文冒充原文。**
3. **新决定的术语没写进 `terms.yml`，这页就不算翻完。** 术语表只增不减，检查器是逼它生长的力。
